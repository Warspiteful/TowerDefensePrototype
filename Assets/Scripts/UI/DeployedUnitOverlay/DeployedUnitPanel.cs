using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class DeployedUnitPanel : MonoBehaviour
{
    [SerializeField] private Image ArchtypeImage;
    [SerializeField] private TextMeshProUGUI UnitName;
    [SerializeField] private TextMeshProUGUI attackState;
    [SerializeField] private TextMeshProUGUI defenseState;
    [SerializeField] private TextMeshProUGUI blockNumber;
    [SerializeField] private TextMeshProUGUI healthBar;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
